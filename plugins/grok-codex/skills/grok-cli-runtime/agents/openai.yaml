interface:
  display_name: "Grok CLI Runtime"
  short_description: "Route Grok tasks through the companion"
  default_prompt: "Use $grok-cli-runtime to run a Grok Build task through the bundled companion."
